# Full read + write + execute envelope, hard-enforced by Gemini's `tools:`
# allowlist. Mirrors the Pi read/edit/write/bash/grep/find/ls set.
tools:
  - read_file
  - write_file
  - replace
  - run_shell_command
  - grep_search
  - glob
max_turns: 50
